# --- ใช้ Ollama Local ---
MODEL=ollama/llama3
OLLAMA_BASE_URL=http://localhost:11434

# --- หรือใช้ Gemini API (คอมเมนต์อันบนออก แล้วเปิดใช้ 2 บรรทัดนี้แทน) ---
# GOOGLE_API_KEY=your_gemini_api_key_here
# MODEL=gemini/gemini-2.0-flash

# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง
# ห้าม commit ไฟล์ .env ที่มี API key จริงขึ้น GitHub
